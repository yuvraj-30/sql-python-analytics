# Dash App

Run:
```bash
pip install -r ../requirements.txt
python app.py
```

This Dash dashboard is aligned with `notebooks/00_interactive_dashboard.ipynb` (ipywidgets) and uses the same sample data from `data/sample/`.
